% with xelatex
\documentclass[border=1pt]{standalone}

%\usepackage[utf8]{inputenc}
%\usepackage[T1]{fontenc}

% \usepackage{arev}

%\renewcommand{\familydefault}{\sfdefault}
%\usepackage[scaled=0.95]{helvet}
%\usepackage[helvet]{sfmath}
%\everymath={\sf}
\usepackage{mathspec}
\setallmainfonts{Arial}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,arrows,automata,positioning}

\begin{document}
\begin{tikzpicture}
    \node[state] (off) at (-2.0,0) {$-$F};
    \node[state] (on) at (2.0,0) {F};
    \draw[every loop, thick]
    (off) edge[loop left,align=right] node {$1-p_I$\\(non-ingress)} (off)
    (on) edge[loop right,align=left] node {$1-p_E$\\(non-egress)} (on)
    (off) edge[bend right] node[below] {$p_I$ (ingress)} (on)
    (on) edge[bend right] node[above] {$p_E$ (egress)} (off);
  \end{tikzpicture}
\end{document}
